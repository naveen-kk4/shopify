package com.naveenk.Shopify.Enum;

// here I have set Coupon as an enum
// there is not much interaction with Coupon (No CRUD operations)
// hence I have set coupon as an enum to save memory and time
public enum Coupon {

    OFF5,

    OFF10,

    NA
}
